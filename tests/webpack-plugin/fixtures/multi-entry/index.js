// Default `main` entry. It imports no client component; Button reaches
// the build through the plugin's injected async block (attached to the
// Flight runtime bundled into this entry).
export const app = 'multi-entry-main';
